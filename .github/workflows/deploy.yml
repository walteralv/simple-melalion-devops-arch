name: Deploy

on:
  workflow_dispatch:

jobs:
  deploy:
    name: Placeholder Deploy Job
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v6

      - name: Deployment placeholder
        run: |
          echo "Add your Terraform apply or deployment commands here."
          echo "This repository now includes a starter Azure + Databricks IaC structure."
